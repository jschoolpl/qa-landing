{
  "name": "jschool-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/index.html --open --port 8080",
    "build": "parcel build src/index.html --no-source-maps --out-dir dist",
    "predeploy": "rm -rf dist && npm run build -- --public-url https://jschoolpl.github.io/qa-landing/"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.49.7"
  }
}
